# @leaderboard/config

environment variables, yaml config loading, and validation.

## exports

- `loadConfig()` - load all config
- `validateConfig()` - validate config
- `getRequiredEnv()` / `getOptionalEnv()` - env helpers
