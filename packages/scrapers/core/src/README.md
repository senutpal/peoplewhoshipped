# @leaderboard/scraper-core

shared types, activity definitions, and base scraper class.

## exports

- `BaseScraper` - abstract base for scrapers
- `GITHUB_ACTIVITY_DEFINITIONS` / `SLACK_ACTIVITY_DEFINITIONS` - activity types
- `getDateRange()`, `isBot()`, `getLogin()` - utilities
