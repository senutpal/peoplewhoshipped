{
  "name": "@leaderboard/scraper-roles",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "dev": "echo 'Roles scraper ready'",
    "build": "bun build src/index.ts --outdir dist --target node",
    "scrape": "bun run src/index.ts",
    "clean": "rm -rf dist",
    "check": "bunx tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@leaderboard/database": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
