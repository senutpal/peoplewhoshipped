# @leaderboard/db-scripts

cli tools for database management.

## commands

- `bun run db:prepare` - init schema
- `bun run db:import` - flat files → database
- `bun run db:export` - database → flat files
