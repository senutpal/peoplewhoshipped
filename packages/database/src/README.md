# @leaderboard/database

pglite database operations for contributors and activities.

## exports

- `getDb()` - singleton db instance
- `addContributors()` / `addActivities()` - insert data
- `getActivitiesByDefinitions()` - query activities
- `updateBotRoles()` - mark bot accounts
